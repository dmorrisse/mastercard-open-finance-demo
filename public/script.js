// script.js — shared navigation helper for Quantum Pay

// Simple navigation shortcut
function go(url) {
  window.location.href = url;
}

// You can simulate Quantum Metric event triggers here if needed later:
// Example:
// QM.recordEvent("bank_connection_error", { bank: "Citi" });
